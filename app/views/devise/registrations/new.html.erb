<div class="d-flex align-items-center flex-column mt-4">
  <h2> Registro </h2>
  <div class="shadow-sm p-3 mb-5 bg-white rounded registro">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control", placeholder:"Correo" %>
      </div>

      <div class="field pt-2">
        <%= f.label :name %><br />
        <%= f.text_field :name , class:"form-control", placeholder:"Nombre" %>
      </div>
      
      <div class="field pt-2">
        <%= f.label :birthday %><br />
        <%= f.date_field :birthday, class:"form-control"%>
      </div>

      <div class="field pt-2">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password", class:"form-control", placeholder:"Contraseña"  %>
      </div>

      <div class="field pt-2">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control", placeholder:"Confirmar Contraseña"  %>
      </div>

      <% if user_signed_in? and current_user.role_id == 7 %>
        <%= f.text_field :role_id, value: 1, hidden: true %>
      <% else %>
        <div class="field pt-2 text-center">
          <%= f.label :role_id %><br />
          <%= f.collection_select(:role_id, user_type, :id, :descr, {},  {class: 'form-control' } ) %>
        </div>
      <% end %>

      <div class="actions text-center pt-2">
        <%= f.submit "Sign up", class: "btn btn-primary" %>
      </div>
    <% end %>

  </div>

  <%= render "devise/shared/links" %>
</div>